name: Set up Minishift

on: [push]

jobs:
  deploy-and-verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Minishift
        run: |
          wget https://github.com/minishift/minishift/releases/download/v1.34.3/minishift-1.34.3-linux-amd64.tgz -O minishift
          chmod +x minishift
          sudo mv minishift /usr/local/bin/
          minishift start --cpus 2 --memory 4096
